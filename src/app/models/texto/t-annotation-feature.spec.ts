import { TAnnotationFeature } from './t-annotation-feature';

describe('TAnnotationFeature', () => {
  it('should create an instance', () => {
    expect(new TAnnotationFeature()).toBeTruthy();
  });
});
