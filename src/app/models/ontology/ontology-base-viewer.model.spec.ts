import { OntologyBaseViewer } from './ontology-base-viewer.model';

describe('OntologyBaseViewer', () => {
  it('should create an instance', () => {
    expect(new OntologyBaseViewer()).toBeTruthy();
  });
});
