import { OntologyObjectPropertyDescription } from './ontology-object-property-description.model';

describe('OntologyObjectPropertyDescription', () => {
  it('should create an instance', () => {
    expect(new OntologyObjectPropertyDescription()).toBeTruthy();
  });
});
