import { OntologyDenoteUsage } from './ontology-denote-usage.model';

describe('OntologyDenoteUsage', () => {
  it('should create an instance', () => {
    expect(new OntologyDenoteUsage()).toBeTruthy();
  });
});
