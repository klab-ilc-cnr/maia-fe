import { OntologyIndividualDescription } from './ontology-individual-description.model';

describe('OntologyIndividualDescription', () => {
  it('should create an instance', () => {
    expect(new OntologyIndividualDescription()).toBeTruthy();
  });
});
