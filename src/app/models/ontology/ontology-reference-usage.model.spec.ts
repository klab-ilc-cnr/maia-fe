import { OntologyReferenceUsage } from './ontology-reference-usage.model';

describe('OntologyReferenceUsage', () => {
  it('should create an instance', () => {
    expect(new OntologyReferenceUsage()).toBeTruthy();
  });
});
