import { OntologyDescriptionField } from './ontology-description-field.model';

describe('OntologyDescriptionField', () => {
  it('should create an instance', () => {
    expect(new OntologyDescriptionField()).toBeTruthy();
  });
});
