import { OntologyClassDescription } from './ontology-class-description.model';

describe('OntologyDescriptionField', () => {
  it('should create an instance', () => {
    expect(new OntologyClassDescription()).toBeTruthy();
  });
});
