import { OntologyAnnotationsField } from './ontology-annotations-field.model';

describe('OntologyBaseViewer', () => {
  it('should create an instance', () => {
    expect(new OntologyAnnotationsField()).toBeTruthy();
  });
});
