import { OntologyAnnotations } from './ontology-annotations.model';

describe('OntologyBaseViewer', () => {
  it('should create an instance', () => {
    expect(new OntologyAnnotations()).toBeTruthy();
  });
});
