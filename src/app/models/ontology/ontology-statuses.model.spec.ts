import { OntologyStatuses } from './ontology-statuses.model';

describe('OntologyStatuses', () => {
  it('should create an instance', () => {
    expect(new OntologyStatuses()).toBeTruthy();
  });
});
