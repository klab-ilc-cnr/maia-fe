import { OntologyPropertyAssertionData } from './ontology-property-assertion-data.model';

describe('OntologyPropertyAssertionData', () => {
  it('should create an instance', () => {
    expect(new OntologyPropertyAssertionData()).toBeTruthy();
  });
});
