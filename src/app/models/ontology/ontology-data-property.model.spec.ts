import { OntologyDataProperty } from './ontology-data-property.model';

describe('OntologyDataProperty', () => {
  it('should create an instance', () => {
    expect(new OntologyDataProperty()).toBeTruthy();
  });
});
