import { OntologyDescriptionAxiom } from './ontology-description-axiom.model';

describe('OntologyDescriptionAxiom', () => {
  it('should create an instance', () => {
    expect(new OntologyDescriptionAxiom()).toBeTruthy();
  });
});
