import { LexiconEditTileContent } from './lexicon-edit-tile-content.model';

describe('LexiconEditTileContentModel', () => {
  it('should create an instance', () => {
    expect(new LexiconEditTileContent()).toBeTruthy();
  });
});
