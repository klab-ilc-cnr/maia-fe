import { LexiconTileContent } from './lexicon-tile-content.model';

describe('LexiconTileContent', () => {
  it('should create an instance', () => {
    expect(new LexiconTileContent()).toBeTruthy();
  });
});
