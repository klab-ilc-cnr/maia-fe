/**Enum dei tipi di elementi del corpus (cartelle e file) */
export enum _ElementType {
  Directory = "directory",
  File = "file"
}

/**Enum of the Corpus element types */
export enum ElementType {
  FOLDER = 'Folder',
  RESOURCE = 'Resource'
}
