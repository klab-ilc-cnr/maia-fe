import { LayerWithFeatures } from './layer-with-features';

describe('LayerWithFeatures', () => {
  it('should create an instance', () => {
    expect(new LayerWithFeatures()).toBeTruthy();
  });
});
