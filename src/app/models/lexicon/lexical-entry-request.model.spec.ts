import { LexicalEntryRequest } from './lexical-entry-request.model';

describe('LexicalEntryRequest', () => {
  it('should create an instance', () => {
    expect(new LexicalEntryRequest()).toBeTruthy();
  });
});
