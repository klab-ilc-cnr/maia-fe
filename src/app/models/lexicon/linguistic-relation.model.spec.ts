import { LinguisticRelationModel } from './linguistic-relation.model';

describe('LinguisticRelationModel', () => {
  it('should create an instance', () => {
    expect(new LinguisticRelationModel()).toBeTruthy();
  });
});
