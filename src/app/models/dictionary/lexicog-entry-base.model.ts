/**Interface of features common to dictionary entries in list and full version */
export interface LexicogEntryBase {
    creator: string;
    lastUpdate: string;
    creationDate: string;
    confidence: number;
}
