import { AnnotationMetadata } from './annotation-metadata';

describe('AnnotationMetadata', () => {
  it('should create an instance', () => {
    expect(new AnnotationMetadata()).toBeTruthy();
  });
});
