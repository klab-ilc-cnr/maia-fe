import { FieldDropdownInput } from './field-dropdown-input';

describe('FieldDropdownInput', () => {
  it('should create an instance', () => {
    expect(new FieldDropdownInput()).toBeTruthy();
  });
});
