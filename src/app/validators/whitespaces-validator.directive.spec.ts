import { WhitespacesValidatorDirective } from './whitespaces-validator.directive';

describe('WhitespacesValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new WhitespacesValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
