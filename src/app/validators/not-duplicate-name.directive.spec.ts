import { NotDuplicateNameDirective } from './not-duplicate-name.directive';

describe('NotDuplicateNameDirective', () => {
  it('should create an instance', () => {
    const directive = new NotDuplicateNameDirective();
    expect(directive).toBeTruthy();
  });
});
