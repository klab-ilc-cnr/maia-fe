import { MatchNewPasswordDirective } from './match-new-password.directive';

describe('MatchNewPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchNewPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
