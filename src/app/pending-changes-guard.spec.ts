import { PendingChangesGuard } from './pending-changes-guard';

describe('PendingChangesGuard', () => {
  it('should create an instance', () => {
    expect(new PendingChangesGuard()).toBeTruthy();
  });
});
